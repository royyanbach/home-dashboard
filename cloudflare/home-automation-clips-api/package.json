{
  "name": "home-automation-clips-api",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "npx wrangler deploy --keep-vars"
  },
  "dependencies": {
    "@pushforge/builder": "^2.0.5"
  }
}
